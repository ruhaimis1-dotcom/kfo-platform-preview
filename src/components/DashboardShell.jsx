export function DashboardShell({children,active='الرئيسية',onNavigate}){const items=[['home','الرئيسية'],['catalog','المسارات'],['reports','التقارير'],['settings','الإعدادات']];return <div className="kfo-dashboard"><aside className="kfo-sidebar"><img src="/upload/logo.png" alt="كفو"/>{items.map(([id,label])=><button key={id} className={active===label?'is-active':''} onClick={()=>onNavigate(id)}>{label}</button>)}</aside><main className="kfo-dashboard__content">{children}</main></div>}